import classNames from 'classnames/bind';
import styles from './PracticeAeaPage.module.scss';

const cx = classNames.bind(styles);

function PracticeAeaPage() {
  return (
    <div className={cx('wrapper')}>
        PracticeAeaPage
    </div>
  );
}

export default PracticeAeaPage;
